# Drawer

`drawer/` holds deferred work that should not enter active execution yet.

Use it for:

- future features
- backlog plans
- ideas that still need shaping before they become desk tasks

## Rule

Work in `drawer/` is not currently executing.

When a drawer item becomes real implementation work, promote it into `desk/` and bind it to the active execution rituals there.
